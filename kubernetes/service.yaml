apiVersion: v1
kind: Service
metadata:
  name: infra-orchestrator-svc
  namespace: sre-system
spec:
  selector:
    app: infra-orchestrator-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
