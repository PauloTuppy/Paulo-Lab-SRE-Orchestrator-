apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ workload }}
  namespace: {{ namespace }}
spec:
  template:
    spec:
      containers:
        - name: app
          livenessProbe:
            initialDelaySeconds: {{ value }}
